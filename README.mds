#include<stdio.h›
long long findMaxTotalPower(int arr[], int n)
long long maxPower = 0;
for(int i =0; i<n; i++){-
if (long long )arr[i] > maxPower )f
maxPower-= (long long)arr [1];
return maxPower * (n +-•1);-
int main()-
int n;
scanf ("%d", &n);
int viedoCards [n]:
for (int i = 0; i ‹ n; i++)
scanf ("%d", &viedoCards [i]);
long long result - findMaxTotalPower (viedoCards, n);
printf ("%11d", result);
return 0; 

